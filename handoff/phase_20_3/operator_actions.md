# Operator Actions

No required operator actions.

Optional follow-up:

- Review `/analytics/opportunities` output against real production-like data before exposing it to merchants.
- Decide whether future merchant dashboards need authentication, billing gates, exports, or scheduled report snapshots.
