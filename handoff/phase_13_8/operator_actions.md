# Operator Actions

## Purpose
Phase 13.8 is implemented in code. The remaining work is review-oriented and focused on the smaller warning set that still remains after volume-and-size tightening.

## Ordered steps
1. Review a sample of the remaining `potential_over_canonicalization_*` warning groups from the latest real archive run.
2. Prioritize still-risky families such as age-band toothpaste, pack-family counts, or other domain-specific numeric families that continue to appear in warnings.
3. Keep canonical products additive-only until downstream consumers have been validated against the further-reduced warning set.
