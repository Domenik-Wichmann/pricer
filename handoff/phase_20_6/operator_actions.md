# Operator Actions

To use locally or in a deployed internal environment:

1. Set `PRICER_INTERNAL_ANALYTICS_TOKEN` for the backend runtime.
2. Open `/internal/insights/dashboard`.
3. Enter the token and use role `admin` or `analyst`.

Do not put the internal analytics token in a public mobile or web bundle.
