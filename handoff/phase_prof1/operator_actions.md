# PROF1 Operator Actions

1. Run `npm run db:migrate`.
2. Optional: run `npm run prof1:build-user-taste-profiles -- --all --json`.
3. Optional: inspect `user_taste_profile_snapshots` and `user_taste_profile_signal_sources` in Postgres.
4. Optional: use `--dry-run` first if you want a preview-only report.

No Firestore, UI, planner, recommendation, or deployment action is required for PROF1.
