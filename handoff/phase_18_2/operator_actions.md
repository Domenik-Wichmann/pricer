# Phase 18.2 Operator Actions

No required operator actions.

Optional follow-up checks:

1. Run the Flutter app against a backend that exposes `GET /products/:id`, `POST /products/deal-check`, and `POST /watchlist`.
2. Tap a home deal and verify product details and Add to watchlist against live data.
3. Decide whether watchlist state should become a toggle in a later phase.
