# DB5A Operator Actions

No required operator action remains for DB5A.

Optional local exploration:
- Configure Postgres through the existing DB1 environment variables.
- Run `npm run db5a:seed-recipe-ingest-staging -- --dry-run --json` to preview the fixture seed.
- Run `npm run db5a:seed-recipe-ingest-staging -- --json` to seed a configured local Postgres database.

