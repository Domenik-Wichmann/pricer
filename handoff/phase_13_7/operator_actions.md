# Operator Actions

## Purpose
Phase 13.7 is implemented in code. The remaining work is review-oriented and focused on the smaller warning set that still remains after year and age-statement tightening.

## Ordered steps
1. Review a sample of the remaining `potential_over_canonicalization_*` warning groups from the latest real archive run.
2. Prioritize still-risky families such as volume-format variants, unresolved age-band groups, or other domain-specific numeric markers that continue to appear in warnings.
3. Keep canonical products additive-only until downstream consumers have been validated against the further-reduced warning set.
