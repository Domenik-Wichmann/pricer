# Operator Actions

## Purpose
Phase 13.5 is implemented in code. Remaining work is review-oriented and focused on the smaller set of canonical warnings that still remain.

## Ordered steps
1. Review a sample of the remaining `potential_over_canonicalization_*` warning groups from real archive runs.
2. Prioritize any still-risky families, especially size-range seafood or other domain-specific variants not yet represented by explicit deterministic markers.
3. Keep canonical products additive-only until downstream consumers have been validated against the tightened warning set.
