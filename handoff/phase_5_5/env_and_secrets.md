# Environment and Secrets

## Environment used
- local Node.js runtime: `v24.14.0`
- package manager: `npm`
- Flutter SDK: not installed in this environment
- Dart SDK: not installed in this environment

## Secrets
- No new secrets were added for Phase 5.5.
- `app/mobile/lib/firebase_options.dart` still contains placeholder values.
- `PRICER_API_BASE_URL` can still be provided via `--dart-define` when running the Flutter app.
