# Operator Actions

No required operator actions for Phase 18.5.

Optional follow-up:

- Run the mobile app against a local or deployed backend and manually verify `/optimize` with live pricing data.
- Decide whether the convenience preference should be exposed as a simple mobile toggle in a later phase.
