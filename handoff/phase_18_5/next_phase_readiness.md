# Next Phase Readiness

Phase 18.5 is ready for follow-on mobile work.

Recommended next targets:

- Saved-list integration: allow saved-list shortcuts to open `/optimize` with list items and list metadata.
- Basket persistence: decide whether draft baskets should be local-only, backend-owned, or saved-list-backed.
- Visual polish: refine store cards, warning presentation, and explanation hierarchy for release UX.
- Convenience UX: add a "prefer fewer stores" toggle only after deciding how to explain convenience-adjusted totals.

Backend changes are not required for those first mobile polish steps unless new saved-list item payloads are needed.
