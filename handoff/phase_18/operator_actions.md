# Phase 18 Operator Actions

No required operator actions are needed to use the functional prototype locally.

Optional follow-up actions:

1. Run the Flutter app against a backend that includes Phase 17.5 `GET /home/summary`.
2. Review the placeholder quick-action behavior and choose final navigation destinations.
3. Schedule a localization/design-polish pass for the new home summary section labels.
