# Operator Actions

No required operator action for Phase 16.1 code verification.

Optional runtime follow-up:
1. Deploy the updated Firebase Functions package when ready.
2. Smoke-test `POST /basket/optimize` against a non-production dataset.
3. Confirm client copy labels `score_total` as ranking score, not user-facing basket cost.
