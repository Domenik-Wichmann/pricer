# Environment and Secrets

## Environment used
- local Node.js runtime: `v24.14.0`
- package manager: `npm`
- Flutter SDK: not installed in this environment
- Dart SDK: not installed in this environment

## Secrets
- No secrets were added to this repository snapshot.
- `app/mobile/lib/firebase_options.dart` contains placeholder values only.
- A real Firebase app configuration must be generated before Firestore-backed mobile flows can run outside the in-memory fallback.
- Optional runtime base URL can be passed with `--dart-define=PRICER_API_BASE_URL=<url>`.
