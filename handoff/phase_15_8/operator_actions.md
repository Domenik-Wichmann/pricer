# Operator Actions

No human operator action is required for the deterministic foundation.

Optional later validation:

1. Deploy or run the backend locally.
2. POST to `/shopping-intent/resolve` with `{ "term": "yogurt" }`.
3. Confirm the response asks for yogurt style before exact product selection.
