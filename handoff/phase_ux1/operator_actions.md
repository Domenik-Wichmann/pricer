# UX1 Operator Actions

None required for this phase.

Optional local verification:

1. `npm run ux1:seed-user-food-profiles -- --dry-run --json`
2. `npm run ux1:seed-user-food-profiles -- --json`

Both commands are Postgres-side only. They do not change UI, Firestore, planner behavior, or runtime recommendations.
