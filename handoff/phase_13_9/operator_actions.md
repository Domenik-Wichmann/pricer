# Operator Actions

## Purpose
Phase 13.9 is implemented in code. The remaining work is review-oriented and focused on the smaller unresolved warning set before any LLM adjudication lane is introduced.

## Ordered steps
1. Review a sample of the remaining `potential_over_canonicalization_*` warning groups from the latest real archive run.
2. Prioritize still-risky families such as Bulgarian count variants, age-band toothpaste variants, and other domain-specific numeric families that remain unresolved.
3. Keep canonical products additive-only until the future LLM disambiguation queue is implemented with durable decision persistence and audit trail.
