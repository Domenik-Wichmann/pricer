# Operator Actions

No required operator action for local development.

When ready to use the new analytics surface outside local development:

- Deploy the updated Firebase Functions package.
- Decide whether analyst tooling should read the default summary, a filtered chain/store summary, or `/analytics/gap-detection/coverage-by-chain` for coverage views.
