# Phase 18.3 Operator Actions

No required operator actions.

Optional manual QA:

1. Launch the Flutter app.
2. On Home, enter `milk` and press search; confirm the `/search` placeholder shows the query.
3. Return Home, enter `milk, eggs` and tap `Add to basket`; confirm the `/optimize` placeholder shows the parsed draft basket.

No secrets, migrations, backend deploys, or external service setup are required for this phase.
