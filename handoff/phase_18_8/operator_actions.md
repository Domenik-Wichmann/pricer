# Operator Actions

No required operator action.

Optional visual QA before release:

- Review the app on small and large mobile devices.
- Check text wrapping on localized Bulgarian copy.
- Decide whether any high-traffic cards need screenshot-based golden tests in a later phase.
