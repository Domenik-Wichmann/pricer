# Operator Actions Template

## Purpose
List only the tasks the human operator must perform because the agent cannot complete them directly.

## Required format
1. Ordered steps
2. Exact values or placeholders needed
3. Click-by-click console instructions where relevant
4. Expected confirmation output
