# Decision Log

## Latest additions
- APP1 exposes meal planning only as thin backend API wrappers over the existing PLAN1 and PLAN2D modules; request handlers may read Postgres sidecar tables directly for detail views, but they must not re-implement planner, requirement, candidate, or optimizer logic.
- APP1 meal-plan shopping runs still depend on the existing runtime store during explicit invocation because PLAN2B and PLAN2C intentionally reuse the current canonical product and price backbone while persisting adapter outputs in Postgres sidecar tables.
- Phase 2L allows `DEFAULT_COORDINATE_MODE` to change nearest-availability defaults only through explicit config; unset or invalid config falls back to `provider_only`, and request `coordinate_mode` remains the strongest override.
- Phase 2K keeps `provider_only` as the nearest-availability default while adding guarded rollout diagnostics and explicit switch criteria for reviewed-coordinate coverage, distance deltas, operator sampling, and rollback.
- Phase 2J keeps nearest availability provider-only by default and requires explicit `coordinate_mode = "reviewed_first"` before active reviewed coordinates can outrank matched provider geocodes; returned offers expose coordinate source for rollout monitoring.
- Phase 2I defined reviewed-coordinate precedence first as diagnostic-only: active reviewed coordinates win, matched provider coordinates win when no active reviewed coordinate exists, and unavailable is returned otherwise.
- Phase 2H publishes approved location-review corrections into `reviewed_location_coordinates` as an additive read model with explicit supersession; raw geocodes/source rows remain unchanged, and consumer nearest availability still ignores reviewed corrections until a later precedence phase.
- Phase 2G exposes location review through guarded internal endpoints that require an explicit admin/operator identity header; these endpoints still write only additive review decisions and do not publish corrections to consumer nearest availability.
- Phase 2F keeps location review corrections additive in `location_review_candidates`; approved coordinates do not overwrite retailer locations, geocode cache rows, manual address text, or saved user location records, and consumer nearest search remains unchanged.
- Phase 2E-3 treats manual-address geocoding as a user-triggered, cache-first enrichment step; raw address text remains preserved, matched coordinates require explicit confirmation before use or saving, and product search/nearest availability never call geocoding implicitly.
- Phase 2E-2 uses foreground device location only after an explicit user tap; acquired coordinates feed the existing opt-in nearest availability path and can be saved only through explicit Home/Work/Custom actions.
- Phase 2E-1 keeps manual address entry as non-geocoded UI text; nearest availability still requires explicit coordinates or an explicit saved-location reference, and the mobile app does not request GPS, call maps, or infer saved places.
- Phase 2D exposes saved locations and nearest availability through explicit API/Flutter flows only; product search remains coordinate-free, and the mobile UI does not request GPS or infer saved locations.
- Phase 2C stores saved user locations only as explicit user-consented preferences; home/work/custom labels are never inferred from behavior, and saved locations only feed opt-in location-aware search resolution.
- Phase 2B keeps nearest-store product availability as an opt-in read helper over matched geocode cache rows; normal product search remains coordinate-independent, and missing/invalid location data returns explicit states instead of falling back silently.
- Phase 2A keeps geocoding additive in `retailer_location_geocodes`, keyed by normalized country/city/address/store identity; provider results never mutate `retailer_locations`, and consumer search, basket, price lookup, and canonical grouping do not depend on coordinates.
- Phase 20.6 serves an unguarded internal dashboard shell but keeps all market-intelligence data requests behind the Phase 20.5 token guard; the shell embeds no token and stores the entered token only in browser localStorage.
- Phase 20.6 is intentionally a dashboard stub rather than a merchant product: no billing gates, export pipeline, merchant scopes, or polished commercial copy are introduced.
- Phase 20.5 uses a temporary shared-secret header guard for internal market-intelligence endpoints because full Firebase Auth claims, merchant scoping, and billing gates are future work; missing guard configuration must deny protected endpoints by default.
- Phase 20.5 keeps token rejection responses intentionally generic (`forbidden`) so deployed endpoints do not reveal whether token config, role, or submitted credentials were wrong.
- Flutter mobile startup renders with local in-memory dependencies before remote bootstrap. Firebase, Firestore, monetization, AdMob, and backend API failures must remain non-fatal to the first frame, while explicit API base URL configuration still overrides local defaults.
- Phase 20.4 keeps merchant/admin insights as a composable read layer over Phase 20 gap summaries and Phase 20.3 opportunity cards; it does not introduce a separate persisted report table or alter lower-level analytics endpoint contracts.
- Phase 20.4 insight `generated_at` is derived from the newest included signal timestamp so outputs remain deterministic for the same source data while still exposing a dashboard-friendly freshness marker.
- Flutter monetization startup treats RevenueCat and AdMob as disabled unless the current platform has real non-placeholder config; local development must boot without RevenueCat keys or AdMob IDs, and placeholder ad unit IDs must not render ad widgets.
- Phase 20.3 keeps market opportunity reports deterministic and evidence-based: reports are computed from existing `gap_signal_store` summaries, never from LLMs, external services, or new persisted recommendation records.
- Phase 20.3 intentionally phrases recommendations as investigation actions with limitations because app interaction signals are not full-market surveys or guaranteed business opportunities.
- Phase 20.2 keeps chain and store segmentation optional: default gap reads stay query/category oriented, while chain/store filters and grouping only apply when explicitly requested or when using chain-coverage summaries.
- Phase 20.2 treats `chain_id` and `store_id` as best-effort observational context derived from request hints or existing source-product/store conventions; missing or ambiguous chain/store context must remain nullable instead of being guessed.
- DB5C promotion explicitly separates canonical existence from runtime eligibility: staged recipes may be promoted into canonical `recipes` with partial ingredient matches, while `recipes.usability_status` is the downstream readiness gate.
- DB5C preserves unresolved recipe evidence instead of blocking promotion outright: unmatched staged ingredient lines stay nullable in canonical `recipe_ingredients`, and `ingredient_gap_candidates` aggregate those gaps for later ingredient-review work.
- Phase 20.1 keeps market-gap locality intelligence deterministic and observational: `locality_code` is nullable signal context, not verified inventory truth, and missing locality must never block signal writes or reads.
- Phase 20.1 preserves the original global gap summary as the default `/analytics/gap-detection` behavior; locality-specific summaries are opt-in via `locality_code`, and multi-locality rollups live on the dedicated `/analytics/gap-detection/localities` read path.
- Phase 18.8 is intentionally a shared-primitive visual polish pass: spacing, radii, typography, empty/error presentation, and small copy refinements are allowed, while backend calls, navigation, data contracts, and new features remain unchanged.
- Phase 18.8 keeps the existing mobile theme architecture and adjusts `AppTheme`, `AppScreen`, `AppSectionCard`, `AppSectionHeader`, `EmptyStateCard`, and `ErrorStateCard` instead of creating a new design system.
- Phase 18.7 mobile saved-list polish uses the existing Phase 17 owner-scoped `/lists` backend contract as the mobile source of truth instead of adding a new client persistence layer.
- Phase 18.7 keeps saved-list optimization transient on mobile: `/list_detail` navigates current items to `/optimize` and intentionally does not store optimizer output or call saved-list optimization during normal screen rendering.
- Phase 18.7 leaves backend behavior, saved-list schema, notifications, and broader client state architecture unchanged; the phase is UI/API-client polish only.
- DB3E creates a reviewable Postgres sidecar bridge from canonical ingredients to purchasable product ids. It deliberately stores product ids as strings because the Postgres product ingestion layer is future work.
- DB3E generated matches are suggestions only: deterministic regeneration may refresh unreviewed rows, but approved and rejected ingredient-product mappings are preserved and never auto-approved.
- Phase 20 keeps market gap detection internal and deterministic: signals are captured from existing user interaction handlers, grouped and scored with fixed constants, and never alter search, resolver, basket, watchlist, canonical, enrichment, or price behavior.
- Phase 20 interprets `missing_supply` as product-not-found demand, `poor_match_quality` as catalog/enrichment resolution quality, and `high_price_pressure` as a pricing opportunity; the endpoint is analytics-only and not a user-facing API surface.
- Phase 18.6 uses the existing backend watchlist tracker price view for mobile `/watchlist`, keeping the screen read-oriented except for the existing `DELETE /watchlist/:id` remove action.
- Phase 18.6 intentionally does not introduce notifications, alert scheduling, analytics, health metrics, or a new client state architecture; target hits and deal labels are displayed from the current price view only.
- DB5B keeps LLM recipe extraction bounded and staging-only: model output must validate as strict JSON, may only write to `recipe_ingest_*` tables, and must never create canonical recipes or ingredients directly.
- DB5B reuses deterministic DB3A ingredient matching after extraction; unmatched or low-confidence ingredients stay staged with nullable `matched_ingredient_id` for later review instead of forcing canonical creation.
- Phase 18.5 makes `/optimize` a real mobile basket optimization route over the existing `POST /basket/optimize` contract, without changing backend optimizer behavior or adding basket persistence.
- Phase 18.5 keeps the mobile optimizer user-facing: it requests explanations but leaves convenience scoring and internal metrics off by default, and omits `score_total`, raw metrics, and debug objects from the UI.
- Phase 18.4 makes `/search` a real mobile product search route over the existing `POST /products/search` contract, while keeping backend search behavior unchanged and leaving filters/facets/pagination for later.
- Phase 18.4 search result cards navigate by canonical product id only; optional deal and best-price fields are displayed when present but are not required for search to work.
- DB5A keeps rich recipe ingest as a raw-preserving Postgres staging layer; staged rows are review evidence and do not create canonical recipes or publish runtime app records.
- DB5A allows nullable staged ingredient matches to existing DB3A ingredients but never auto-creates ingredients; promotion into canonical recipes remains an explicit future review phase.
- Phase 18.3 keeps home search and basket draft entry lightweight: search submits only navigate to `/search`, add-to-basket only parses comma/newline-separated text and navigates to `/optimize`, and no basket data is persisted.
- Phase 18.3 leaves `/search` and `/optimize` as argument-aware placeholders so the top home input can be validated before full destination screens are built.
- DB4C promotes recipe nutrition profile candidates into approved recipe nutrition profiles only through a Postgres sidecar review workflow; no recipe nutrition is published to Firestore/runtime in this phase.
- DB4C allows only one approved recipe nutrition profile per recipe; approving a replacement candidate supersedes the prior approved profile and preserves review history.
- Phase 18.2 keeps product detail as a focused Flutter route over existing backend APIs: `GET /products/:id`, `POST /products/deal-check`, and `POST /watchlist`. Deal-check is treated as optional secondary data so product detail remains usable when pricing/deal signals are unavailable.
- Phase 18.1 uses Flutter's existing `Navigator` and a small named-route registry instead of introducing Router, deep-link handling, or a larger state architecture. Existing list and watchlist screens are reused, while search, optimize, and product destinations remain lightweight placeholders until their full mobile workflows are defined.
- DB4B recipe nutrition candidates are Postgres sidecar review candidates calculated only from `recipe_ingredients.quantity_grams` and approved `ingredient_nutrition_profiles`; recipes still do not map directly to USDA rows.
- DB4B preserves candidate review status on regeneration and skips recipes with zero valid nutrition inputs instead of inventing ingredient nutrition, creating ingredients, or publishing runtime nutrition.
- DB4A keeps canonical recipes in Postgres sidecar tables linked only to existing DB3A ingredients; fixture seeding skips missing ingredient keys instead of auto-creating ingredients.
- DB4A recipe rows and recipe ingredient lines do not carry USDA FDC IDs; nutrition must flow later through ingredient nutrition profiles, not direct USDA recipe mappings.
- Phase 18 keeps home-screen integration inside the existing Flutter search/home screen and API client rather than adding new backend behavior or app state architecture. The screen consumes `/home/summary` as a read-only app-facing feed, hides empty dynamic sections, and uses placeholder quick-action taps until navigation polish is scheduled.
- Future schema work must use `docs/SCHEMA_MAP.md` as the relationship/source-of-truth atlas alongside `docs/DATA_MODEL.md`; schema boundary changes require doc updates and decision-log entries.
- Future implementation sessions must use `docs/REPO_MAP.md` as the repo navigation schema, keep it synchronized with module/schema/test ownership changes, and update stale docs when repo reality differs.
- Phase 17 saved shopping lists persist user input only; optimization results are intentionally not cached, and each saved-list optimization request reruns the existing basket pipeline fresh.
- Phase 16.7 treats basket health as internal diagnostics only; alerts are computed from summaries, never written back to analytics records, and must not affect optimizer behavior or normal user-facing product flows.
- DB1 introduces Postgres only as sidecar foundation: connection helpers, migration tooling, health checks, and general import metadata tables.
- DB1 keeps the existing `kolkostruva.bg -> Firestore/flat store -> App` product runtime authoritative and does not switch product, shopping, watchlist, or basket read paths.
- DB1 migration tracking uses a repo-owned `schema_migrations` table with SQL-file checksums and deterministic filename ordering.
- DB2 imports USDA/FoodData Central macro nutrition into Postgres sidecar tables only; no nutrition data is published to Firestore or app-facing runtime in this phase.
- DB2.5 generates deterministic USDA food cluster candidates only for Foundation and SR Legacy foods; USDA foods still must flow through cluster/review layers before any Pricer ingredient nutrition mapping.
- DB2.5 excludes branded foods from first-pass clustering so packaged products stay product-side until the Open Food Facts / barcode enrichment path.
- DB2.5 candidate keys include hard-boundary qualifiers for raw/cooked state, grain/flour form, juice/sauce forms, milk fat, meat cut/cooking/skin/bone/breading, sweetening, salting, and drained/liquid state to avoid over-collapsing nutrition-distinct foods.
- DB2 filters `food_nutrient.csv` to nutrient IDs `1008`, `1003`, `1004`, `1005`, `1079`, `2000`, `1093`, `2047`, and `2048` instead of importing all nutrient rows blindly.
- DB2 stores `usda_foods.food_category_id` as text because actual USDA `food.csv` rows can contain source category text, not only numeric category ids.
- DB2 normal tests use a tiny fixture dataset; the full USDA import remains an explicit operator/dev command against configured Postgres.
- DB1 import metadata is intentionally general (`source_datasets`, `source_files`, `import_batches`) so DB2 can add USDA-specific schemas without mixing source truth with runtime read models.
- Phase 16.6 persists basket analytics only as an explicit opt-in after metrics are computed; persistence failures must not block optimizer responses, and summaries must ignore malformed records rather than changing basket behavior.
- Phase 16.5 keeps basket quality metrics as an observation-only layer: metrics may summarize resolver, pricing, optimization, and convenience outputs, but they must not change optimizer behavior, mutate inputs, persist analytics, call external services, or appear in `/basket/optimize` unless explicitly requested.
- DB0 amendment: Postgres must be introduced as an additive sidecar, not a replacement; the existing `kolkostruva.bg -> Firestore -> App` product runtime remains authoritative through DB1 and early DB phases.
- DB1 must not rewrite `phase1/store.js`, change product ingest behavior, remove Firestore usage, move canonical products, alter product search, alter shopping-list resolution, or affect basket output.
- Existing product data may be mirrored into Postgres later for parity checks, historical aggregation, trends, and analytics, but migration of product read paths requires verified data parity, tests proving identical or accepted outputs, tested fallback, production performance validation, and no pricing or basket regressions.
- Firestore keeps current/live app-facing data and user state; Postgres should carry historical price snapshots, long-range aggregates, source audit history, trends, analytics, and heavy external dataset processing.
- Phase DB0 adopts a hybrid persistence boundary: Postgres should own future relational source truth, heavy imports, USDA/Open Food Facts joins, source dedupe, and mapping-review staging; Firestore/flat store remains the app-facing cache and user-state runtime.
- DB0 must be design-only; Postgres dependencies, migration runners, and live connection handling begin in DB1, not in the architecture phase.
- Future dataset ingest must preserve raw source truth, run deterministic identity and dedupe first, enrich only net-new or enrichment-missing entity concepts, cache enrichment, review uncertainty, and publish runtime-safe read models.
- DB2.5F creates ingredient nutrition mapping suggestions only from approved `usda_food_clusters`; raw `usda_foods` never map directly to Pricer ingredients, and suggested mappings require explicit review before runtime use.
- DB2.5F stores `ingredient_id` as a string bridge instead of a Postgres foreign key because the Pricer ingredient catalog is still flat-store/runtime led; a future sidecar ingredient mirror can feed suggestions without moving canonical ingredient ownership yet.
- User-facing/searchable food entities must support `name_bg`, `name_en`, `aliases_bg`, and `aliases_en`; raw third-party names are source truth, not runtime search truth.
- Phase 16.3 keeps explanation as a pure read-only translation layer over optimizer output; it must not alter ranking, scoring, price selection, or persistence.
- Phase 17.1 treats missing saved-list owner fields as anonymous for backward compatibility while enforcing owner-scoped access for all new CRUD and optimize flows.
- Phase 17.5 keeps the mobile home feed as a read-only consumer aggregation layer over existing user-facing helpers, intentionally excluding basket health, analytics internals, optimizer debug output, pipeline logs, and notification delivery.
- Phase 17.5 saved-list cards are shortcuts only; the home summary must not run basket planning or optimization for every saved list.
- Phase 17.4 keeps market trends as a read-only internal insight layer over canonical enrichment and product daily prices; it does not mutate canonical, enrichment, price, optimizer, saved-list, or watchlist records.
- Phase 17.4 computes deal density by reusing the Phase 17.3 deal classifier, keeping deal semantics consistent across watchlist, basket, and market summary surfaces.
- Phase 17.2 keeps the new watchlist tracker separate from Phase 9 watchlist intelligence profiles; tracker records are owner-scoped canonical-product references while Phase 9 remains the future alert/insight layer.
- Phase 17.3 treats deal detection as a lightweight deterministic signal layer, not an alert-rule engine; current price is compared with available recent average using fixed thresholds.
- Phase 15 diet and attribute claim normalization should be deterministic, explicit-text-only, and additive to `canonical_enrichment_store`; it must not infer claims from categories or mutate canonical products, mappings, or grouping.
- Phase 6 store-location extraction should derive `retailer_locations` from existing `store_name_raw`/source-file provenance only, leaving coordinates null until a bounded geocoding enrichment phase and never changing product identity, search, basket, price lookup, or canonical grouping behavior.
- Phase 17.3 annotates watchlist and basket read outputs without persisting deal outputs, sending notifications, mutating price records, or requiring user configuration beyond optional target prices.
- Phase 17.2 uses Phase 16.0 price lookup as the source of truth for current watchlist prices and does not persist price snapshots, best-price results, or tracker read-view outputs in `watchlist_store`.
- Phase 17.1 uses temporary `x-pricer-owner-id` and `x-pricer-owner-type` headers only as an auth-prep boundary; real token validation and user account lifecycle remain future work.
- Phase 17.1 continues to store saved-list user input only and does not persist resolver, price, optimizer, explanation, metrics, analytics, or health outputs.
- Phase 16.4 keeps convenience scoring as a separate user-context layer; `actual_total` remains pure product price and `effective_total` carries convenience penalties.
- Phase 16.4 does not estimate real travel distance or time yet; configured penalties are explicit placeholders until store geocoding and routing exist.
- Phase 16.2 should recommend a multi-store split only when it improves or preserves coverage, meets configured actual savings, and does not worsen penalty-adjusted score.
- Phase 16.2 keeps travel, distance, time, delivery, and locality costs out of scope until a later locality-aware optimization phase.
- Phase 16.1 should rank single-chain basket options with an internal missing-item penalty while keeping user-facing `actual_total` limited to real known EUR prices.
- Phase 16 source prices are treated as EUR; the backend must not perform BGN defaults, currency conversion, or exchange-rate calls in price lookup or basket optimization.
- Phase 16.1 may auto-select carried ambiguous candidates only under an explicit cheapest-candidate policy and must surface that choice as a warning.
- Phase 16.0 should expose current price truth through a deterministic canonical lookup layer over existing snapshots and mappings, not by inventing a new canonical-price store.
- Phase 15.4 should keep basket-input planning as a pure deterministic classification layer between shopping-list resolution and optimization, preserving request signals without performing pricing or persistence.
- Phase 15.3 should resolve shopping-list intent through the product-service layer with deterministic ranking and explicit resolved, ambiguous, or unresolved outputs instead of introducing a separate canonical join path.
- Phase M0 keeps meal ingredients as a separate domain with explicit bridge mappings from `canonical_products`; retailer-product truth must not be repurposed as ingredient truth.
- Phase M0 owns unit conversion, ingredient-specific conversion rules, and ingredient-level price projection inside the meal domain first, even if some helpers become shared later.
- Ingredient-level price projection should preserve explicit fallback provenance in this order: exact local mapped price, other-store mapped price, category-average estimate, then ingredient estimate.
- Phase 15.2 should expose the first product-facing consumers through bounded API contracts that default to `canonical_with_enrichment` while keeping applied disambiguation opt-in only.
- Phase A inspection concludes that meal intelligence must be added as a parallel domain with explicit bridge layers, not by collapsing ingredients into `canonical_products` or by forcing recipe ingest into the retailer-price ingest pipeline.
- The repo's active persistence truth is the flat backend store plus Firestore-compatible collections; relational migrations and Data Connect are not current implementation anchors for meal-domain planning.
- Any future meal-domain backend work must preserve the duplicated deploy/runtime backend trees under both `functions/src/` and `app/functions/src/`.
- Phase 15.1 should expose enrichment only through explicit reader layer selections so downstream code cannot accidentally blur canonical truth, applied view, and additive enrichment.
- Phase 15 should keep semantic enrichment additive and cache-first by canonical fingerprint, never a mutation of deterministic canonical truth or marker precedence.
- Phase 14.3 should apply canonical disambiguation decisions only as a reversible audited view; canonical products, source identity, and deterministic grouping state remain immutable truth in this phase.
- Phase 14.2 should make human canonical disambiguation reviews the highest-trust effective decision while preserving prior LLM decision provenance and still avoiding live canonical mutation.
- Phase 14.1 should keep LLM canonical disambiguation opt-in, cache-first, strictly schema-validated, and provenance-only until a later explicit decision-application phase.
- Phase 14.0 should persist unresolved canonical warnings as durable queue records with stable pair fingerprints and a separate reusable decision store before any live LLM merge application is attempted.
- Phase 13.9 should classify numeric families contextually so count, age-band, and reserve-tier markers become hard canonical boundaries while bare ambiguous numbers remain unresolved for later review.
- Phase 13.8 should treat normalized volume and weight markers as canonical-variant boundaries, including ml-versus-liter, cl-versus-ml, kg-versus-g, and conservative bare-decimal volume expressions.
- Android and iOS app identifiers now converge on `com.pricer.mobile`, and FlutterFire/Firebase CLI regenerated the Android+iOS Firebase config against `pricer-ee440`.
- Firebase Cloud Functions should run from `europe-west1`, and repo-controlled backend URL examples should point at `https://europe-west1-pricer-ee440.cloudfunctions.net/api`.
- Phase 13.7 should treat vintage years and age statements as canonical-variant boundaries whenever deterministic year or age markers are clearly present.
- Phase 13.6 should treat explicit numeric range markers as canonical-variant boundaries, including slash-separated and unit-suffixed ranges.
- Phase 13.5 should tighten canonical keys with explicit deterministic variant markers such as stages, age bands, flavors, colors, and pack counts when those markers are clearly present.
- Cross-chain canonical product grouping must be additive above stable source-product identity and chain-plus-product-code dedupe, never a replacement for either layer.
- DB3A canonical ingredients live in Postgres as sidecar Pricer ingredient truth for future nutrition profiles, while current product/search/shopping/basket runtime paths remain on the existing flat runtime stores.
- DB3A ingredient IDs are stable Pricer IDs derived from canonical ingredient keys; they are not USDA FDC IDs, and raw USDA foods continue to connect only through reviewed clusters and ingredient nutrition mappings.
- DB3C ingredient nutrition profile candidates are preview/review sidecar records derived only from approved ingredient nutrition mappings and USDA macro rows; generation must preserve existing profile review status and must not publish runtime nutrition.
- DB3D approved ingredient nutrition profiles are review workflow outputs only; replacement approvals supersede the previous approved profile for the same ingredient, mapping type, and default state instead of overwriting history.
- Phase 13 canonical keys should prefer under-grouping to over-grouping and use deterministic enrichment attributes only, without embeddings or LLM calls in the grouping path.
- PROF1 taste snapshots should read canonical recipe metadata plus the latest approved staged recipe metadata through recipe promotion history so flavor/texture/feeling/method signals stay deterministic, auditable, and append-only without mutating canonical recipe rows.
- Phase 6 pre-enrichment dedupe should reuse one enrichment result per `source_chain_name_normalized + "::" + product_code`, with a safe fallback to stable source-product identity when that bucket key cannot be formed.
- KolkoStruva CSV filenames are treated as additional provenance metadata for chain-level grouping and debugging, never as primary product identity.
- Daily KolkoStruva ZIP archives must be processed across all supported CSV entries sequentially; duplicate suppression is archive-wide rather than per-file.
- Firebase deployment should use a repo-root `functions/` source package that vendors its runtime dependencies under the deploy tree instead of importing backend code from `app/functions/src`.
- Backend persistence now uses an environment-selectable store contract: Firestore for production runtime, JSON files for local CLI work, and in-memory state for tests.
- The Firestore adapter preserves the existing flat collection shapes instead of introducing a new backend schema.
- Flutter localization should keep ARB source files separate from generated Dart outputs; the repo now generates `AppLocalizations` into `app/mobile/lib/src/generated/l10n/`.
- Phase 12 keeps canonicalization deterministic and conservative, preferring missed rewrites over risky rewrites.
- Phase 7 demand aggregates may teach search-quality typo mappings only when a single best canonical token is clearly identifiable.
- Phase 11 treats deployment as an audit-first phase because the repo still has production blockers beyond missing secrets.
- Deployment readiness must distinguish operator-owned setup from codebase gaps such as local JSON persistence, missing function wrappers, and missing auth or notification registration paths.
- Phase 10 keeps backend entitlement records flat and backend-authoritative instead of trusting client-only premium flags.
- Alert gating stays backward-compatible for legacy unsynced users so earlier watchlist flows keep working until RevenueCat sync is present.
- RevenueCat and AdMob remain environment-configured; default Google test app ids are acceptable for local development but not for release.
- Phase 9 builds on Phase 6 drop detection and daily price history instead of creating a separate alert-evaluation path.
- Low notification spam is enforced through deterministic per-item cooldowns and daily summary aggregation.
- Basket-level and watchlist-level weak-signal handling should be conservative, preferring no nudge over a noisy nudge.
- Phase 8 reuses Phase 4 ranked item results instead of rebuilding a separate basket-matching path.
- Basket optimization stays deterministic and bounded by explicit candidate-store and combination limits to avoid combinatorial blowups.
- Phase 8 applies a basket-level minimum match-score floor so ultra-weak query results do not silently become basket picks.
- Phase 7 logs unmet demand only after the deterministic Phase 4 query flow returns zero results; matched queries are not duplicated into the demand layer.
- Manual "can't find this" reports should write both the existing `feedback_events` record and a new Phase 7 demand log so human feedback and automatic zero-result capture stay comparable.
- Demand clustering remains batch-based and deterministic, using token-derived embeddings rather than runtime AI calls.
- `MASTER_PRODUCT_SPEC.md` was missing, so the repo now includes a source-product-backed spec aligned with the implemented data model.
- Phase 6 uses the existing stable source-product identity for ingest dedupe because the repo data model has no separate `store_id` field.
- Phase 6 keeps live provider configuration environment-driven instead of hardcoding Grok, embedding, Firebase, or FCM credentials into the repo.
- The Flutter app should detect placeholder Firebase options without relying on a custom property that would disappear when a real generated `firebase_options.dart` replaces the placeholder.
- Phase 5.6 keeps localization strictly in the Flutter UI layer; backend product and store content remain backend-driven.
- English is the safe UI fallback locale when the device locale is unsupported.
- Shared formatting helpers should read the active Flutter locale instead of hardcoding English-style output.
- Phase 5.5 keeps growth hooks inside the existing mobile client instead of adding new backend endpoints by default.
- Savings, reruns, and watchlist urgency should be visible on-screen before any secondary metadata.
- The shared UI system is intentionally lightweight and screen-driven rather than introducing heavyweight app-wide state or design-system tooling.
- Phase 5 reuses the existing `/query` and `/product/:id/history` contracts instead of creating mobile-specific backend endpoints.
- Anonymous device-local identity is sufficient for the initial Firestore list and watchlist paths; full authentication remains out of scope.
- The repository keeps a checked-in placeholder `firebase_options.dart` and falls back to in-memory repositories until real Firebase settings are generated on a Flutter-enabled machine.
- Phase 4 is a composition layer over existing phases rather than a rewrite.
- SQL and vector sync targets remain flat and idempotent.
- Deterministic matching remains primary and AI stays fallback only.
- PLAN2D should stay a thin orchestration wrapper over PLAN1 plus PLAN2A through PLAN2C; it must not introduce a second shopping or optimizer stack.
- Because the existing PLAN1 and PLAN2 modules chain through persisted deterministic sidecar ids, PLAN2D `dry_run` suppresses orchestration-run persistence but still lets lower-level sidecar artifacts follow their normal upsert path so the pipeline can remain end-to-end and adapter-only.
