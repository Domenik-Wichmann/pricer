# Prompt — Create Repo Scaffold

Read `README.md`, `AGENTS.md`, `docs/PHASE_PLAN.md`, and `docs/implementation/PHASE_0_IMPLEMENTATION.md`.

Create or update the repo foundation for this Bulgarian grocery price-alert app.

Requirements:
- preserve the repo structure already defined in docs
- do not import SystemWright-specific files or concepts unless explicitly present in this repo
- implement the Phase 0 scope only
- keep code and docs readable
- run `npm run verify` and `npm run validate:docs`
- produce `handoff/phase_0/` with:
  - operator_actions.md
  - verification_report.md
  - files_changed.md
  - env_and_secrets.md
  - next_phase_readiness.md

Operator action rule:
Only assign tasks the human cannot perform automatically through the coding environment.
