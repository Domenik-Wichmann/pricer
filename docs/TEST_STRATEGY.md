# Test Strategy

## Principles
- Each phase defines its own acceptance checks.
- Tests should be runnable locally where possible.
- Imported data should have fixture-driven tests before live source integration.
- Every phase must register both automated and manual verification output.

## Required artifacts per phase
- test registry update
- test run file under `docs/test_runs/`
- handoff verification report
